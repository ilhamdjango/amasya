spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zire?useSSL=false&serverTimezone=UTC
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  application:
    name: zire_fk

server:
  port: 8080



  web:
    resources:
      static-locations: classpath:/static/