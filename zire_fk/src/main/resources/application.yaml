spring:
  datasource:
    url: jdbc:postgresql://dpg-d363u09r0fns73bjv1d0-a:5432/zire
    username: zire_user
    password: 6EPKcfSsjpwdxGBV3796YxFo9DrmY0oN
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  application:
    name: zire_fk

  profiles:
    active: prod

  thymeleaf:
    cache: true

server:
  port: ${PORT:8080}
  web:
    resources:
      static-locations: classpath:/static/

logging:
  level:
    root: INFO

file:
  upload-dir: /app/uploads   # ✅ burada əlavə et
